# 正向预测与逆向重构温度历史参数 —— 代码原理

## 整体架构

该项目研究**聚合物玻璃的退火行为**（以聚苯乙烯 PS 为例）。一个两段式热历史由四个参数定义：

- **T₁**：第一段退火温度
- **t₁**：第一段退火持续时间
- **T₂**：第二段退火温度
- **t₂**：第二段退火持续时间

代码分两条主线：

| 模块 | 数据来源 | 物理模型 | 目标 |
|------|----------|----------|------|
| `models/` | 文献数字化图表数据 | ARRT + TNM | 预测退火响应（ΔH、记忆效应） |
| `ps/` | 真实 PS-DSC 仪器实验数据 | 特征工程 | 预测焓恢复特征 |

---

## 一、正向预测（Forward Prediction）

**输入**：热历史参数 (T₁, t₁, T₂, t₂)

**输出**：退火响应（ΔH、ΔH_peak、S*、记忆效应标志等）

### 1.1 物理特征工程

#### ARRT（绝对反应速率理论 / Eyring 方程）

文件：`src/physics/arrt.py`

弛豫速率由过渡态理论给出：

$$k = \frac{k_B T}{h} \cdot \exp\left(\frac{S^*}{R}\right) \cdot \exp\left(-\frac{H^*}{RT}\right)$$

核心逻辑：
- 从退火时间近似反应速率：`k ≈ 1/t`
- 反推活化焓 H*，作为预测特征而非直接拟合参数
- `activation_enthalpy_from_entropy(T, t, S*)` 实现：从温度、时间和活化熵推导 H*

#### TNM（Tool-Narayanaswamy-Moynihan）模型

文件：`src/physics/tnm_features.py`

描述玻璃态结构弛豫的经典唯象模型：

$$\tau(T) = A \cdot \exp\left(\frac{H^*}{RT}\right) \quad \text{（Arrhenius 弛豫时间）}$$

$$\phi(t) = \exp\left(-\left(\frac{t}{\tau}\right)^\beta\right) \quad \text{（KWW 拉伸指数）}$$

无量纲弛豫剂量：

$$D(T, t) = 1 - \exp\left(-\left(\frac{t}{\tau(T)}\right)^\beta\right)$$

该剂量值 ∈ [0, 1]，代表在该温度下达到的弛豫完成程度。

使用文献拟合常数：
- H* = 164 kJ/mol
- A = 6×10⁻²² s（指前因子）
- β = 0.43（拉伸指数）
- x = 0.6（非线性参数）

### 1.2 数据集构建

文件：`src/models/dataset.py`

从文献数字化图中提取数据点（Fig.2a-c, Fig.2d/S6, S7, Fig.3b），为每个样本计算 **17 维特征向量**：

| 类别 | 特征 | 物理含义 |
|------|------|----------|
| **原始参数** | T₁, T₂, ΔT, log(t₁), log(t₂), 1/T₁, 1/T₂ | 热历史的直接编码，温度倒数对应 Arrhenius 坐标 |
| **ARRT 导出** | H*₁ (arrt), H*₂ (arrt) | 从 T, t, S* 通过 Eyring 方程反推的活化焓 (kJ/mol) |
| **Fig.3 经验关系** | H*₁ (fig3), H*₂ (fig3), Tp₁, Tp₂ | 文献中 H*(Tp) 的经验关系，补充物理约束 |
| **TNM 导出** | dose₁, dose₂ | 拉伸指数弛豫剂量，量化各段退火的弛豫程度 |
| **熵特征** | S*₁, S*₂ | 从文献 S7 图中查得的活化熵 |

**5 个目标变量**：ΔH（焓恢复量）、ΔH_peak（峰值焓）、S*₂（第二段活化熵）、ΔS*（熵变）、memory_effect_label（记忆效应二分类标签）

评估标准：ΔH_peak ≥ 0.05 且 ΔH ≥ 0.4 时标记为存在记忆效应。

### 1.3 RBF 核回归器

文件：`src/models/kernel_regression.py`

核心是一个 **Nadaraya-Watson 核回归**（非参数方法，适合小样本）：

**训练**：Z-score 标准化所有特征，保留训练集的均值和标准差。

**预测**（高斯核加权平均）：

$$w_{ij} = \exp\left(-\frac{\|x_i - x_j\|^2}{2\sigma^2}\right)$$

$$\hat{y}_i = \frac{\sum_j w_{ij} \cdot y_j}{\sum_j w_{ij}}$$

**不确定性**（加权标准差）：

$$\sigma_{\hat{y}_i} = \sqrt{\sum_j w_{ij} \cdot (y_j - \hat{y}_i)^2}$$

关键超参数：bandwidth σ = 1.25, ridge = 1e-6。

### 1.4 模型训练与评估

文件：`src/models/train_forward.py`、`src/models/evaluate_split.py`

- 使用 **LOO（留一法）** 交叉验证评估，输出每个目标的 MAE
- 对比两组特征：**基线组**（不含 Fig.3 的 H*/Tp）vs **增强组**（全部特征），验证物理先验的增益
- `evaluate_split.py` 实现分组划分（按 panel + t₂ 分组防泄漏），输出 MAE / RMSE / R²

### 1.5 PS 实验数据线

文件：`src/ps/dsc_processing.py`、`src/ps/train_ps_model.py`

与 `models/` 线并行，处理真实 DSC 仪器数据：

| 步骤 | 说明 |
|------|------|
| 解析 Excel | 读取仪器元数据块、温度程序段、Time/Temp/DSC/DDSC 信号列 |
| 信号校正 | 用参比扫描 (ref) 做基线校正，用空皿扫描 (empty) 估计噪声水平 |
| 去趋势 | 在 40–160°C 窗口内线性去趋势 |
| 特征提取 | 峰面积（梯形积分）、峰温 Tp、峰高、起始温度、总焓恢复 ΔH_total |
| 衍生指标 | recovery_index（相对于同 T₁ 最大恢复的归一化值）、path_dependence_index（两步与最近单步的差值） |

正向模型特征（7 维）：`is_two_step, T1_C, T2_C_filled, delta_T_C, log_t1_s, log_t2_s, log_total_time_s`

目标（8 维）：焓恢复总量、峰面积、峰温、峰高、起始温度、恢复指数、路径依赖指数、Kovacs 峰标记。

---

## 二、逆向重构（Inverse Design）

**输入**：期望的目标属性（如 ΔH = 0.55 kJ/mol, ΔH_peak = 0.12 kJ/mol, memory_effect = 1）

**输出**：能产生这些属性的最优热历史参数 (T₁, t₁, T₂, t₂)

### 2.1 算法流程

文件：`src/models/inverse_design.py`、`src/ps/inverse_design.py`

```
┌─────────────────────────────────────────────────────────┐
│  Step 1: 加载已训练的正向预测模型（RBF 核回归器）         │
├─────────────────────────────────────────────────────────┤
│  Step 2: 生成候选参数网格                                │
│    models 线:                                            │
│      T₁ ∈ {348, 353, 358, 363, 368, 373} K              │
│      T₂ ∈ {373, 378, 383} K  (约束 T₂ ≥ T₁)             │
│      t₁, t₂ ∈ {0.1, 0.2, 0.5, 1, 2, 5, 10, 20,         │
│                 50, 100, 200, 500, 1000} s               │
│      → 约 2000+ 个候选组合                               │
│    ps 线（更精细）:                                       │
│      T ∈ {50, 55, ..., 100} °C (11 个温度)              │
│      t ∈ {10, 30, 60, ..., 1800} s (9 个时间)           │
│      → 单步 99 + 双步 数千个候选                          │
├─────────────────────────────────────────────────────────┤
│  Step 3: 对每个候选计算：                                 │
│    a) 物理特征向量（ARRT + TNM 特征工程）                 │
│    b) 正向模型预测所有目标值                              │
│    c) 加权多目标损失：                                    │
│       L = (1/N_active) · Σ |pred_i - target_i| / scale_i │
├─────────────────────────────────────────────────────────┤
│  Step 4: 按损失排序，返回 Top-K 最优热历史参数             │
└─────────────────────────────────────────────────────────┘
```

### 2.2 关键设计要点

1. **不是训练逆向模型**：不尝试学习 `目标属性 → 热历史` 的映射（这通常是不适定的逆问题），而是**暴力网格搜索 + 正向模型评估**。正向模型推理极快（矩阵运算），毫秒级可评估数千候选。

2. **多目标归一化**：各目标使用不同的特征尺度归一化：
   - ΔH：scale = 0.08
   - ΔH_peak：scale = 0.04
   - S*₂：scale = 30
   - memory_effect_label：scale = 1.0

3. **时间惩罚**（PS 线）：损失函数中加入 `0.05 × total_time / 1800` 项，倾向于更短实验时间的方案，兼顾实验效率。

4. **约束条件**：T₂ ≥ T₁（第二步温度不低于第一步），t ≤ 1800s（单步不超过 30 分钟，总实验可行）。

---

## 三、核心原理总结

```
                       正向预测
   热历史 (T₁,t₁,T₂,t₂)  ──────────→  退火响应 (ΔH, ΔH_peak, S*, ...)
                              │
                 ┌────────────┼────────────┐
                 │  ARRT 推导 H*           │
                 │  TNM 计算弛豫剂量        │  ← 物理先验注入
                 │  Fig.3 经验关系          │
                 └─────────────────────────┘
                              │
                       RBF 核回归器
                    (非参数, 小样本友好)

                       逆向重构
   期望目标属性  ──────────→  最优热历史参数
                 │
    候选网格枚举 → 正向预测 → 加权损失排序 → Top-K
```

### 方法学本质

**物理信息驱动的稀疏核回归**（Physics-Informed Sparse Kernel Regression）：

- 用 ARRT 和 TNM 将领域知识**编码为特征**，而非直接对原始参数做黑箱拟合
- 即使在只有几十个数据点的极度稀疏条件下，模型仍能捕捉玻璃态弛豫的非线性、路径依赖性
- 逆向设计利用正向模型的廉价评估能力，通过**穷举搜索**绕过逆问题的不适定性

### 记忆效应的物理解释

两段式退火中：
1. 第一步在低温 T₁ 下长时间退火 → 形成局部有序结构（低能构型）
2. 第二步在高温 T₂ 下退火 → 部分擦除第一步的结构记忆
3. 当 T₁ < T₂ 且 t₁ 足够长时，DSC 升温扫描出现**双峰**：
   - 低温峰：对应 T₁ 温度区间的结构弛豫（"记忆峰" / Kovacs 记忆效应）
   - 高温峰：对应 T₂ 温度区间的主体焓恢复

这是玻璃态物理中典型的 **Kovacs 记忆效应**——系统的当前状态不仅取决于最近的温度历史，还"记住"了更早期的退火历程。

---

## 四、代码文件索引

| 文件 | 功能 |
|------|------|
| `src/physics/arrt.py` | ARRT 反应速率理论：速率估算、活化焓推导、Eyring 方程 |
| `src/physics/tnm_features.py` | TNM 模型：弛豫时间、KWW 拉伸指数剂量 |
| `src/models/kernel_regression.py` | RBF 核回归器（Nadaraya-Watson） |
| `src/models/dataset.py` | 从文献数字化图构建退火数据集（17 特征 + 5 目标） |
| `src/models/train_forward.py` | 正向模型训练 + LOO 评估 |
| `src/models/evaluate_split.py` | 分组训练/测试划分 + 回归指标评估 |
| `src/models/inverse_design.py` | 逆向设计：网格搜索最优热历史参数 |
| `src/ps/dsc_processing.py` | DSC 仪器数据解析 + 焓恢复特征提取 |
| `src/ps/experiment_design.py` | 构建有限时间 PS 实验矩阵 |
| `src/ps/train_ps_model.py` | PS 正向模型训练 |
| `src/ps/inverse_design.py` | PS 逆向设计搜索 |
